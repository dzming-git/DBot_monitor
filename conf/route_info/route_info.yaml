# 主程序配置
service:
  name: DBot_monitor
  ip: 0.0.0.0
  port: 5704
  tags:
    - DBot
    - DBot_monitor
    - python
  endpoints:
    receive_command: receive_command

# 机器人主程序配置
bot:
  name: DBot_main

# 消息代理配置
message_broker:
  name: DBot_message_broker
  endpoints: 
  
    message_broker_endpoints: endpoints  # 与主程序保持一致
    # 其他endpoint从message_broker_endpoints中获取
    # app.py
    service_commands: ''
    service_endpoints: ''

    # message_sender.py
    service_results: ''